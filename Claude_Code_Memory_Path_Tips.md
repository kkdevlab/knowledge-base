# Claude Code メモリー保存先の仕組み Tips

Claude Code（CLI / VS Code拡張機能 共通）のメモリー・セッション保存先に関する知見。

---

## 2026-08-07: プロジェクトフォルダをリネーム/移動すると、既存のメモリーが「消えた」ように見える

- **症状**: これまで蓄積してきたはずのメモリー（`memory/MEMORY.md` 等）が、新しいチャットで空っぽに見える。ユーザーが「以前ここに書いたはず」と言っても、Claude側は「メモリーには何もない」と答えてしまう
- **原因**: Claude Codeは作業ディレクトリ（cwd）の**絶対パスをそのままエンコードした文字列**をキーにして、`~/.claude/projects/<エンコードされた絶対パス>/` 配下にセッション・メモリーを保存する。この仕組みはCLI・VS Code拡張機能で共通（同じ`~/.claude/`を参照する）。プロジェクト横断・グローバル共通のメモリー領域は存在しない
  - 例: `C:\Users\k_ken\OneDrive\Documents\kklab` → `c--Users-k-ken-OneDrive-Documents-kklab`
  - フォルダ名の一部を変更・移動すると、エンコード後の文字列も変わり、**Claude Codeからは「別プロジェクト」として扱われる**。その結果、新しい（空の）メモリーフォルダが使われ、旧フォルダに保存されていた内容は見えなくなる
  - 特に**マルチバイト文字（日本語等）を含むパスをASCII名にリネームする**ようなケースで発生しやすい。日本語部分はエンコード時に`-`へ置換されるため、リネーム前後でエンコード結果が全くの別物になる
- **解決方法**: `~/.claude/projects/` 配下を一覧し、該当プロジェクト名に近い（エンコードパターンが似ている）旧ディレクトリを探す。見つかったら、その `memory/` フォルダの中身を、現在の（新しい）エンコードパス配下の `memory/` へ手動コピーする
  ```powershell
  Copy-Item -Path "C:\Users\<user>\.claude\projects\<旧エンコードパス>\memory\*" -Destination "C:\Users\<user>\.claude\projects\<新エンコードパス>\memory\" -Force
  ```
- **注意**: 新しい方の `memory/` フォルダ自体が作成されていないケースもある（プロジェクトを開いた直後でまだ何も書き込んでいない場合）。その場合は先にフォルダを作成してからコピーする
- **再発防止策**: プロジェクトフォルダ自体を今後リネーム・移動する予定がある場合は、作業前後で `~/.claude/projects/` 配下のメモリーフォルダも合わせて移行することを忘れないこと
