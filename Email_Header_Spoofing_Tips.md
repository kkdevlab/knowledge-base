# メールヘッダーの偽装手口・検出Tips

送信ドメイン・SPF/DKIM/DMARC等を検証しても見逃す種類のなりすまし手口と、その検出方法の知見。

---

## Unicode双方向テキスト制御文字（RLO等）によるFrom表示名の視覚的偽装

- **手口**: メールのFrom表示名に、Unicode双方向テキスト制御文字（RLO: U+202E 等）と、視認させたい文字列を**逆順**に並べた文字列を組み合わせることで、メールクライアント上では正規のブランド名（例: "Amazon.co.jp"）に見えるが、実際の文字コード列は逆順（例: "pj.oc.nozamA"）という偽装が可能になる（Unicode BiDi override／"Trojan Source"系の攻撃と同種の手口）
- **検出の落とし穴**: 表示名に対する単純な部分一致（例: `text.contains("amazon")`）では、文字列が逆順に格納されているため一致せず検出漏れになる
- **検出方法（実装が軽量な順）**:
  1. **制御文字自体の存在チェック（最も簡単・確実）**: LRE/RLE/PDF/LRO/RLO（U+202A-202E）やLRI/RLI/FSI/PDI（U+2066-2069）が表示名に含まれているかを確認する。正規のメール送信者がFrom表示名にこれらの制御文字を使う理由は通常ないため、**存在自体を強い根拠として扱ってよい**（LRM/RLM = U+200E/200F は正規利用もあるため除外推奨）
  2. 文字列を実際に双方向テキストアルゴリズムで逆順評価してからブランド名と比較する方法もあるが、実装コストが大きい。上記1で大半のケースは検出できる
- **備考**: DocomoMailGuardAndroidの固定ルールエンジン（`FixedRuleEngine.kt`）実装時に、実際に届いていたフィッシングメール（Amazonを偽装、送信ドメインは無関係な第三者ドメイン）で検出漏れを確認し、対策として1の方式を追加した（2026-07-16）。この用途ではAI（LLM）による補助判定は表示名の視覚的な意味を理解できるため正しく検出できていたが、固定ルール側でも軽量に検出できることが判明した
