# Tasker Scene V2 公式マニュアル（ローカル保存版）

**出典**: https://tasker.joaoapps.com/userguide/en/scenes_v2.html
**取得日**: 2026-03-18
**セクション数**: 19

---

## 01. 概要（Overview）

Tasker Screen Builder は、コーディングなしでカスタムスクリーンを作成できるビジュアルデザインツール。

**主な機能:**
- 入力フォーム・ダッシュボード・コントロールパネルのビジュアルエディタ
- コンポーネントベース（Column / Row / Box などコンテナ、Text / Button / Image などコンテンツ要素）
- JSONが内部構造（直接コード編集も可能）
- フルスクリーン・フローティングダイアログ・アプリオーバーレイ等の表示モード

**インタラクティブ機能:**
レイアウトをTaskerタスクに接続し、変数とProvidesシステムでダイナミックデータを扱える。

---

## 02. クイックスタート（Quick Start）

1. **レイアウト作成** — SCENESタブから "Simple Layout"（空白キャンバス）or "Full Page Layout"（プリセット構成）を選択
2. **テキスト編集** — プレビューパネルでテキストコンポーネントを選択・内容変更（例：「Hello, World!」）
3. **コンポーネント追加** — Elements ツリーの「+」でコンポーネントを追加（例：Textの後にButton）
4. **インタラクション設定** — Buttonタップ時にレイアウトを閉じるなどの動作を設定
5. **プレビュー・デプロイ** — プレビューパネルでサイズ確認後、「Show Scene v2」アクションで展開

> アンドゥ機能あり。基本を習得したら Full Page Layout テンプレートも探索推奨。

---

## 03. エディタ概要（Editor Overview）

| タブ | 役割 |
|------|------|
| Elements | コンポーネントツリー（ファイルエクスプローラー形式）。選択・並び替え・「+」追加 |
| Properties | 選択コンポーネントの属性編集（General / Provides / Modifiers） |
| Interaction | ジェスチャーハンドラー設定（タップ・長押し・スワイプ）・ハプティクス |
| Raw JSON | リアルタイム同期バリデーション付きJSON編集。一括編集・レイアウト共有に便利 |
| AI | 自然言語でレイアウト生成・修正 |
| Test Variables | `showWhen` の条件付き表示を一時変数で確認（実行なしでプレビュー） |

**Previewパネル**: リアルタイムレンダリング。マルチディスプレイ時はサブ画面に移動可能。

**デザインモード:**
- **Screen mode** — 完全なシーン
- **Element mode** — スクリーンラッパーなしの再利用可能コンポーネントテンプレート

---

## 04. レイアウト基礎（Layout Fundamentals）

### 3つの基本コンテナ

| コンテナ | 説明 |
|---------|------|
| **Column** | 子要素を縦方向に配置（上→下）。ルートコンポーネントとして最も多用 |
| **Row** | 子要素を横方向に配置（左→右）。ツールバー・横並びコンテンツに最適 |
| **Box** | 子要素を重ねて配置。オーバーレイ・バッジ・画像テキスト重ねに使用 |

### ネストの原則
コンテナを組み合わせることで複雑なレイアウトを実現：
- Column + Row = グリッド状レイアウト
- Column（Rowヘッダー + Columnボディ）= アプリライクなレイアウト
- Box（Image + Textオーバーレイ）= スタイルカード

### 位置制御
| コンテナ | 配置オプション |
|---------|--------------|
| Column | horizontal alignment（Start / Center / End）+ vertical spacing |
| Row | horizontal spacing + vertical alignment |
| Box | 9ポジションのコンテンツ配置システム |

`SpaceBetween` は端に要素を配置しつつ均等スペースを入れるのに便利。

---

## 05. コンポーネント（Components）

### カテゴリ

| カテゴリ | コンポーネント |
|---------|--------------|
| **レイアウト** | Column, Row, Box, Scaffold, TopAppBar, BottomAppBar, Spacer, Variable |
| **表示** | Text, Image, Divider |
| **入力** | Button, IconButton, Switch, TextInput, FloatingActionButton |

### 重要概念

**コンポーネントID**: 自動生成されるが変更可能。変更するとエディタが全参照を自動更新。インタラクション設定・ライブ更新に使用。

**条件付き表示（showWhen）**: boolean式でコンポーネント表示を制御。`false`の場合は非表示（hidden）ではなくレイアウトから**完全除去**される。

**スロット**: コンテナは名前付きスロットで子を受け取る（例：Scaffoldは topBar / bottomBar / floatingActionButton / content スロットを持つ）。

### 重要な制限
TextInputはオーバーレイモードでは動作しない（Android制限でキーボード入力不可）。フルスクリーンまたはダイアログモードを使用すること。

---

## 06. モディファイア（Modifiers）

### コアコンセプト

> 「モディファイアは上から下の順に適用される。各モディファイアはその前のものを包む」

例：パディング → 背景色の順に適用 vs 背景色 → パディングの順：前者は色の内側にスペースが入り、後者は色の外側に透明スペースが入る。

### 5つの機能グループ

| グループ | モディファイア |
|---------|-------------|
| **Sizing** | Size, FillWidth, FillHeight, Weight |
| **Spacing & Position** | Padding, Offset, Align |
| **Visual Style** | Background, Border, Clip, Shadow, Rotation, 透明度 |
| **Scrolling** | オーバーフロー制御 |
| **Parent-Scoped** | Weight, Align（特定のコンテナ内でのみ有効） |

> 表示結果がおかしい場合、まずモディファイアの順序を確認すること。

---

## 07. カラー & テーマ（Colors & Theming）

### カラー形式

| 形式 | 例 | 特徴 |
|-----|---|------|
| Hex（固定） | `#FF5722` | デバイスに関係なく固定 |
| Hex（透明度付き） | `#80000000` | `#AARRGGBB` 形式 |
| テーマカラー | `primary`, `surface`, `error` 等 | ライト/ダークモードに自動対応 |

### テーマカラーの種類
primary / surface / secondary / tertiary / error状態 / invertedバリアント など

### ベストプラクティス
- 標準UIにはテーマカラーを使用（ユーザー設定を自動尊重）
- ブランド固有の色や装飾的要素のみ Hex カラーを使用
- 親コンポーネントの背景色を変えた場合、子コンポーネントは自動調整されないため手動で色を設定する必要がある

---

## 08. Provides システム（Provides System）

名前付きの値をコンポーネント階層に注入し、子孫が `@{key}` 構文で参照できる仕組み。

### スコープの種類

| スコープ | 説明 |
|---------|------|
| **Screen-Level** | スクリーン全体で利用可能。テーマカラーや設定フラグに最適 |
| **Component-Level** | コンテナが宣言し、その子のみに適用。親と同じキーを子がシャドウすると子の値が優先（親側は影響なし） |

### 参照の書き方
`@{key}` 構文はすべての文字列プロパティで使用可能：
- コンポーネントプロパティ（text, color, textSize, hint 等）
- モディファイアプロパティ（color, width, radius 等）

### 解決の優先順位
親コンテナ → 外側の祖先 → スクリーンレベル。キーが見つからない場合は `@{...}` テキストがそのまま表示される（プレビューで未設定が視覚的に分かる）。

**注意**: トランジティブ解決（Provides値が他のProvidesキーを参照）は可能。ただし循環参照は解決されず未解決のまま残る。

---

## 09. 変数（Variables）

### 2つの仕組み

**Variableコンポーネント（構造プレースホルダー）:**
実行時に内容で置き換えられる。キー（例：`%content`）を指定し、TaskerがプレーンテキストまたはJSON配列で値を充填。複数アイテムに展開される場合、IDは自動的に `_1`, `_2` などのサフィックスが付く（サブツリー全体でユニーク化）。

**文字列変数の置換:**
`%` プレフィックスのTasker変数をテキストやプロパティに直接埋め込める。

| 変数の種類 | 特徴 |
|----------|------|
| **グローバル変数**（大文字含む、例：`%VOLM`） | 自動更新。追加アクション不要でレイアウトに即反映 |
| **ローカル変数**（全小文字） | 発生元タスクのスコープ内。更新には **Update Scene v2** アクションが必要 |

`%` 変数と `@{key}` provides参照は同じレイアウト内で併用可能。

---

## 10. 条件付き表示（Conditional Visibility）

### showWhen プロパティ

Tasker変数に対するboolean式でコンポーネントの表示/非表示を制御。

### 演算子

| 演算子 | 種類 |
|-------|------|
| `==`, `!=`, `>`, `<`, `>=`, `<=` | 比較（数値 or 文字列） |
| `!` | NOT（最高優先度） |
| `&` | AND（ORより高優先） |
| `|` | OR（最低優先度） |

括弧でデフォルト優先度を上書き可能。

### 重要な制限
> 「`showWhen` 式は **Tasker の `%variables` のみ** から解決する」

Provides値（`@{key}`）はテキストとカラープロパティでのみ解決され、showWhenでは使えない。

### テスト方法
エディタのフラスコアイコン → Test Variablesパネルで変数値をシミュレート（実行なしで表示動作を確認）。

---

## 11. イベントハンドラー & アクション（Event Handlers & Actions）

### 設定手順
1. Elements ツリーでコンポーネントを選択
2. Interaction タブを開く
3. 「Add Handler」をクリック

### ジェスチャーの種類

| ジェスチャー | 説明 |
|------------|------|
| Single Click | 標準タップ |
| Long Press | 長押し |
| Multi-Click | 複数回の素早いタップ |
| Swipe | 方向スワイプ |

### アクションの種類

| アクション | 説明 |
|----------|------|
| Command | Taskerの「Command」イベントが認識する名前付き文字列を送信 |
| Task | 特定のTaskerタスクを変数渡し付きで実行 |

### 特殊機能
- **ハプティクスフィードバック**: 振動応答を独立して有効化
- **Drag to Move**: 指定コンポーネントでオーバーレイウィンドウをドラッグ移動
- **Dismissal**: レイアウトを閉じ、オプションで呼び出しタスクに結果値を返す
- **Result bindings**: TextInputとSwitchは、Dismiss時に自動的に値を渡せる

1つのコンポーネントに複数ハンドラー（異なるジェスチャー）を割り当て可能。

---

## 12. Taskerアクション（Tasker Actions）

### 動的コンテンツ表示
`%BATT` 等のTasker変数でリアルタイムデータを表示。**Variable**コンポーネントで実行時にテキストまたはコンポーネントツリー全体を置換できる。

### タスクトリガー時の自動変数

タスクが呼び出されると以下の変数が自動的に渡される：

| 変数 | 内容 |
|-----|------|
| `%sv2_screen_name` | スクリーン識別子 |
| `%sv2_element_id` | トリガーした要素のID |
| `%sv2_element_value` | 要素の現在の状態値 |

スワイプジェスチャーの場合：`%swipe_path_x()` / `%swipe_path_y()`（ジェスチャーパスの座標配列）

### ライブ更新アクション

| アクション | 用途 |
|----------|------|
| **Update Scene v2** | テキスト・色・表示を変更（レイアウトを閉じずに更新） |
| **Update Scene v2 Overlay** | オーバーレイの位置・サイズをアニメーション付きで調整 |

ライブダッシュボード・プログレスインジケーター・ドラッグ可能パネル等に活用できる。

---

## 13. スクリーンプロパティ（Screen Properties）

### 主要設定

**名称設定**: エディタとTaskerのアクションピッカーに表示。空白の場合は "Screen" がデフォルト。JSONの最上位フィールドとして保存。

**Provides**: キーバリューペアをコンポーネントツリー全体に提供（Taskerからレイアウトへのデータ注入）。

### システムバーの制御

| 設定 | 内容 |
|-----|------|
| **Colors** | ステータスバー/ナビゲーションバーの色（Hex or テーマ名：`primary`・`surface` 等） |
| **isLightStatusBars** | ライト背景使用時に有効化（アイコンの視認性を維持） |
| **isLightNavigationBars** | 同上（ナビゲーションバー用） |
| **hideSystemBars** | 没入型モード（メディア・ゲームに最適） |

### 重要な制限
> 「システムバー設定は **Fullscreen と Dialog の表示モードのみ** に適用される」

オーバーレイは他アプリの上に浮かぶため、システムバーを制御できない。

---

## 14. 表示モード（Display Modes）

### 3つの主要表示モード

| モード | 説明 | 用途 |
|-------|------|------|
| **Fullscreen** | スタンドアロンアクティビティとして表示 | ダッシュボード・フルページフォーム等の複雑なスクリーン |
| **Dialog** | フローティングウィンドウ（背景のアプリが見える） | クイック確認・入力プロンプト・通知 |
| **Overlay** | 他アプリの上に表示（「他アプリの上に表示」権限が必要） | 最も柔軟。どのアプリの上にも表示可能 |

### 共通機能

| 機能 | 説明 |
|-----|------|
| **Auto-dismiss** | 指定ミリ秒後に自動クローズ（全モード対応） |
| **Continue Task Immediately** | レイアウトの閉鎖を待たずにタスクを継続（常時表示の浮遊コントロール等に有効） |
| **Long Click Root to Dismiss** | オーバーレイモードのみ。ルートを長押しでDismiss |

### IDベーストラッキング
既存IDでレイアウトを表示するとそのレイアウトが**インプレースで更新**される。新しいIDは別表示として起動。

マルチディスプレイ対応（折りたたみ機種・外部モニター等で特定画面に表示可能）。

---

## 15. オーバーレイ機能（Overlay Features）

### 位置・サイズ設定

| 単位 | 説明 |
|-----|------|
| `dp`（密度非依存ピクセル） | 固定位置 |
| `%`（パーセンテージ） | 画面相対位置 |

コンテンツの自動ラップまたは明示的サイズ指定が可能。

### タッチ動作
デフォルトはオーバーレイがタッチをインターセプト。タッチブロックを無効化すると「タッチが下のアプリに透過」（情報表示用に便利）。

### アニメーション
11種類のプリセット（FadeIn / BounceIn 等）。表示時と非表示時で独立して設定可能。

### インタラクティブ機能
コンポーネントをドラッグ可能にすることで、ユーザーがオーバーレイウィンドウを移動できる。

### 上に表示（Show Over Everything）
アクセシビリティサービスを使用し、ロック画面等の制限されたシステムサーフェスの上にも表示可能。

### 重要な制限
> 「TextInputコンポーネントはオーバーレイモードでは動作しない」（Android制限）

テキスト入力が必要な場合はフルスクリーンまたはダイアログモードを使用。

---

## 16. AI レイアウト生成（AI Layout Generation）

### 使い方
1. AIタブに切り替え
2. 欲しいレイアウトを自然言語で説明
3. 「Generate」をタップ

### 機能

| 機能 | 説明 |
|-----|------|
| コンポーネント選択状態での編集 | Elementsツリーで選択中に「このテキストを大きく」等と指示できる |
| フルレイアウト返却 | 他の要素も調整される場合がある |
| アンドゥ | AI修正も通常編集と同様に元に戻せる |

### AIメニュー
オーバーフローメニューからシステムインストラクションをコピー・エクスポートし、ChatGPTやClaudeなど他のLLMで使用可能。

### プロンプトのコツ
- 構造的な詳細を具体的に記述（曖昧な指示は避ける）
- インタラクションと動作にフォーカス
- 個別コンポーネントを後から細かく調整して反復
- 色は特別な理由がなければ指定しない（デフォルトでMaterial Youテーマカラーを使用）

---

## 17. Raw JSON 編集（Raw JSON Editing）

### 双方向同期
> 「すべてのビジュアル変更がJSONを更新し、すべてのJSON変更がビジュアルエディタを更新する」

### 主なユースケース
- レイアウトの共有・インポート（JSONのコピー）
- 複数コンポーネントへの一括変更（色の一括置換等）
- レイアウト問題のトラブルシューティング（コンポーネント設定の直接確認）

### ステータスインジケーター

| 状態 | 色 | 意味 |
|----|---|------|
| Valid | 緑 | 正常なJSON。ビジュアルエディタと同期中 |
| Invalid | 赤 | 構文エラー。解決するまでビジュアルエディタの更新が停止 |

### JSONの構造
```json
{
  "name": "スクリーン名",
  "root": {
    "type": "Column",
    "children": [...],
    "modifiers": [...]
  }
}
```

コンポーネントはtype名（"Column" / "Row" / "Box" 等）を参照。色はHex値またはテーマ名を使用。

---

## 18. Tips & パターン（Tips & Patterns）

### 共通レイアウトパターン

| パターン | 説明 |
|---------|------|
| **Scaffold** | フルスクリーンアプリの基盤。topBar / bottomBar / FAB / content 領域が自動でオーバーラップを回避 |
| **カード** | 角丸コンテナ + 背景色 + パディング |
| **設定パネル** | ラベル行 + Switch の組み合わせ（設定オーバーレイに最適） |
| **グリッド** | Weightモディファイアで等幅列（レスポンシブ対応） |
| **中央寄せ** | Boxコンテナ + 中央寄せ（ヒーローセクション・焦点部分） |
| **データドリブンリスト** | Variableコンポーネントで動的コンテンツを表示 |
| **条件レンダリング** | `showWhen` 式で状態に応じたUI切替 |
| **画像背景** | Box重ねレイヤー（画像 + 暗いスクリム + テキスト） |
| **オーバーレイパネル** | コンパクトな浮遊インターフェース（角丸 + 最小パディング） |
| **テーブルレイアウト** | Weightベースのセルで構造化データ整理 |

### ベストプラクティス
- 完全なスクリーンレイアウトには Scaffold から始める
- 色のハードコードより Material You のデフォルトを活用
- インタラクティブなアイコンには IconButton を使用（適切なタッチターゲット確保）
- 固定サイズよりWeightベースのサイズでレスポンシブ設計
- コンポーネント階層は浅く保つ
- インタラクティブな要素には意味のあるIDを付与
- 柔軟なスペースには Spacer + Weight を使用
- 再利用可能な値には `provides` を活用
- 状態依存のUIには `showWhen` 条件を適用

---

## 19. 比例スケーリング（Proportional Scaling）

### 概要
異なる画面サイズでも一貫した比率を維持するため、デバイスの実際の幅に基づいて寸法値を自動スケーリング。

### 有効化の手順
1. **基準幅の設定** — スクリーンプロパティ > Scaling で基準画面幅（dp）を設定（スマートフォンでは 360dp が一般的）
2. **`pr` サフィックスの適用** — 寸法値に `pr` を付加

> 「実行時に `pr` サフィックスを持つ値は `(currentScreenWidth / referenceWidth)` を乗算される」

### 使用可能なプロパティ
テキストサイズ / パディング / 幅・高さ / 制約 / 位置 / ボーダー / シャドウ / ブラー / Divider / Spacer 等

### 注意事項
- 基準幅が未設定の場合、`pr` は通常の寸法と同じ動作になる（スケーリングなし）
- alpha / weight / rotation は比例スケーリングに対応しない（画面サイズに関係なく一定であるべき）
- `pr` と通常値を同じレイアウト内で混在可能（例：ボーダー厚は一定のままタイポグラフィをスケール）
