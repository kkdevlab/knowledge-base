# Windows.Media.Ocr Tips

## 2026-08-10: Windows.Media.Ocr は小さいUI要素（十数px程度の文字）の認識が不安定

- **症状**: 小さくクロップした画像（例: 136x181px、文字高さ11〜13px程度）を Windows.Media.Ocr にかけると、文字が全く認識されない、または1文字ずつバラバラの「行」として認識されることがある
- **原因**: OCRエンジンの実用的な最小文字高さを下回っている。極小フォント（促音「ッ」等）はさらに認識率が低い
- **対策1（認識率向上）**: 画像を数倍（3倍程度）に拡大（`Graphics.DrawImage` + `InterpolationMode.HighQualityBicubic`）してから認識させると改善する。ただしぼかしが強い高倍率（5倍等）や `NearestNeighbor` はノイズ文字を誘発することがあり、逆効果になる場合がある
- **対策2（座標のズレ）**: 拡大画像で認識した `OcrWord.BoundingRect` の座標は拡大した分だけスケールされるため、元の座標系で使うには倍率で割り戻す必要がある（忘れるとクリック位置等がズレる）
- **対策3（読み順）**: `OcrResult.Lines` の順序は必ずしも視覚的な読み順（左→右、上→下）と一致しない。特に文字がバラバラの「行」として認識された場合、`Top`/`Left` 座標で明示的に並べ替える必要がある
- **対策4（欠落文字への耐性）**: 対策1〜3を行っても、極小フォントの一部文字だけは安定して認識されないことがある。完全一致を要求せず、期待する文字列が順序を保ったまま一定割合以上認識できていれば一致とみなす「あいまいサブシーケンスマッチ」を検出ロジックに入れると実用上のロバスト性が上がる
- **備考**: コンソールアプリから `Windows.Media.Ocr`（WinRT API）を直接使う場合、`TargetFramework` を `net*-windows10.0.xxxxx` にするだけで動作し、`Microsoft.WindowsAppSDK` パッケージは不要
