# Claude Code (VS Code拡張) の MCP サーバー接続タイミングに関する知見

## 現象: セッション開始直後、特定のMCPサーバーが「未接続」扱いになりチャット内では直らない

- VS Code拡張機能を起動した直後に新規チャットで作業を始めると、設定済みのはずのMCPサーバー（例: Notion）のツールが使えないことがある
- 同じ設定のまま新しいチャットタブを開き直すと、今度は正常に使える
- 途中で設定変更は一切していない

## 原因（GitHub Issueから裏付け）

MCPサーバーへの接続は、VS Code拡張機能の起動タイミングに対して**非同期**に確立される。接続処理はウィンドウ（拡張機能ホストプロセス）単位で1回進行し、複数のチャットタブ間で共有される（チャットタブごとに再接続しているわけではない）。

- **[Issue #45293](https://github.com/anthropics/claude-code/issues/45293)**: 「MCP server connections are global -- toggling a server on/off affects all concurrent sessions」と明記。接続がウィンドウ単位で共有されることの裏付け
- **[Issue #25751](https://github.com/anthropics/claude-code/issues/25751)**: VS Code起動直後にMCPハンドシェイクが完了しないと、エラー表示なしでサイレントに失敗する。「Developer: Reload Window」で２回目のハンドシェイクは成功する（バックエンドが既にウォームアップされているため）と報告されている
- **[Issue #57932](https://github.com/anthropics/claude-code/issues/57932)**: （`SessionStart`フックの`mcp_tool`タイプ限定の話だが）セッション開始とMCP接続完了の間に実際に約280〜290msのギャップがあることをログで確認できる。フックがMCP接続より先に発火してしまうため`mcp_tool`タイプのフックは**MCP疎通チェック用途には使えない**
- **[Issue #64671](https://github.com/anthropics/claude-code/issues/64671)**: Claude Desktop（Claude Codeとは別プロダクト。メンテナーも「Code側の話ではなさそう」として`NOT_PLANNED`でクローズ）だが、参考になる技術的知見として、MCPサーバーのプロセスspawnにハードコードされた5秒タイムアウトがあり、複数サーバーが同時起動要求されるとOS側でシリアライズされ、キューの後ろのサーバーほどタイムアウトしやすいと報告されている。MCPサーバーを多数設定しているWindows環境ほど発生しやすい可能性

## 実際の挙動モデル（未接続時、そのチャット内では回復しない）

1. VS Code / 拡張機能起動と同時に、各MCPサーバーへの接続がバックグラウンドで順次進行し始める
2. チャットで最初にツールを使おうとした**その時点**で、接続済みのMCPサーバーだけがそのチャットで使えるものとして固定される
3. その時点で未接続だったサーバーは、**同じチャット内で後からいくら待っても・再送しても使えるようにならない**（tool availabilityはチャットごとに最初の時点でスナップショットされ、後から更新されない）
4. 新しいチャットタブを開くと、その分だけ時間が経過しているため、裏の接続がより多く完了している → そこで初めて使えるようになる

→ つまり「新規チャットを開くと直る」のは、新規チャット自体に意味があるのではなく、**開いた時点で十分な時間が経過していたから**、という理解が実態に近い。

## 対処法（本リポジトリのユーザーが採用した設計）

MCPの生死判定を `SessionStart` フックの `mcp_tool` タイプで行うと上記 #57932 と同じ罠にはまる（早すぎて誤検知する）。代わりに、Claude自身が**最初のユーザーメッセージに応答する時点で見えているツール一覧（deferred tools含む）を確認し、期待するMCPサーバーのツールが存在するかを比較する**方式であれば、実際のMCP呼び出しを伴わず、かつタイミング問題を回避できる。

グローバル `~/.claude/CLAUDE.md` に、「新規セッション最初のメッセージで、必須MCP（例: Notion）のツールが見当たらない場合は、ユーザーの入力内容には着手せず、状況報告と『続行する/新しいチャットを開き直す』の選択だけを提示し、明示的な返答を待つ」という指示を追加することで実現できる。
