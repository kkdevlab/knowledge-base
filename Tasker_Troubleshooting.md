# Tasker トラブルシュート

Tasker 利用中に遭遇したエラー・警告の解決記録（汎用的な挙動・不具合）。
同じ問題が発生した場合はここを参照する。プロジェクト固有のトラブルは各プロジェクトの `CLAUDE.md` を参照。

---

## 2026-03-23: Input Dialog の Default フィールドで二段階展開（%%var）が動作する

- **確認内容**: `%%par1`（par1にグローバル変数名の文字列が入っている）を Input Dialog の Default フィールドに指定
- **結果**: 正常に動作。%par1 の値をグローバル変数名として解釈し、その値を Default に表示する
- **活用場面**: Edit タスクで現在のグローバル変数値をダイアログのデフォルト値として表示する場合、中間変数（%current_val等）は不要

---

## 2026-02-13: Tasker で %id が変数名として使用不可

- **エラー内容**: `Error: %id: must be a variable or array name.`
- **原因**: Tasker のローカル変数名は3文字以上必要（%id は2文字で短すぎる）
- **解決方法**: `%target_id` など3文字以上の名前に変更する
- **備考**: Tasker のローカル変数命名ルール: 全小文字、3文字以上

---

## 2026-05-29: 6.7.3-beta 更新後に %DATE のフォーマットが変わる

- **症状**: `%DATE` が `22-04-2026` → `4-22-26` のように変化する。再起動で一時的に直るが再発する
- **原因**: ベータ更新によるアプリ言語設定のリセット
- **解決方法**: Android 設定 → アプリ → Tasker → 言語 → 正しいロケール（例: English (Belgium)）を選択
  - Tasker の設定（Preferences）ではなく Android のシステム設定から変更すること
- **備考**: joaomgcd 確認済み。Android 13+ の System language picker とも連携

---

## 2026-05-29: Scene V2 ランドスケープ回転時にシーンサイズが崩れる

- **症状**: ポートレートで起動したシーンをランドスケープに回転すると、サイズ・位置が崩れる
- **原因**: Show Scene V2 アクションの Width / Height をピクセル値で指定していると回転時に対応できない
- **解決方法**: Width / Height はパーセント指定（例: 100%）にする。ピクセル値は使わない
- **備考**: joaomgcd 回答で確定（6.7.3-beta で修正済みのバグも含む）

---

## 2026-05-29: Scenes V2 インタラクションが 6.7.3-beta 更新後に動作しない

- **症状**: 既存シーンのボタンクリックなどのインタラクションが動作しない
- **原因**: 6.7.3-beta で Event Handler の仕組みが全面刷新された（Interactable モディファイア方式廃止）
- **解決方法**: 各コンポーネントを開いて新しい Event Handler 形式で再設定する
  - ボタンクリック → Event Handler: Set Variable or Run Task
  - テキスト値取得 → Output to Variable
  - シーンを閉じる → Dismiss Layout
- **備考**: Result Binding も廃止。`ScenesV2_Dev_Guide.md` セクション0 を参照

---

## 2026-07-13: Running Log で End If が記録されるかどうかは採用された枝（If側/Else側）で非対称

- **確認内容**: `Sub_ReturnCode_Flash`（If/Flash/Else/Flash/End If という単純な1段のIf構造）を、
  Par1を変えて2回連続で単体実行し `runlog.txt` を比較した。
  - **If側（先に条件が成立する枝）が採用されたとき**: `If` → `Flash`(If側の中身、実行) → `Else`
    まで記録され、**End If の行は記録されずそのまま `ExitOK`** になる
  - **Else側（最後の枝）が採用されたとき**: `If` → （If側の中身はスキップ）→ `Else` → `Flash`(Else側の
    中身、実行) → **End If まで記録**されてから `ExitOK` になる
- **結論**: 「End If が必ず記録されない」わけではない。**If側が採用された場合にのみ End If の行が
  省略され、Else側が採用された場合は End If まで記録される**という非対称な挙動。
  どちらの場合も採用された枝のアクション（Flash等）自体は正しく `OK` で記録されており、
  分岐判定・実処理自体は正常（Else 側採用時に End If が省略されないことから、これは Running Log の
  取りこぼしではなく Tasker のIf/Else/End If実行モデル自体の挙動と考えられる）。
- **注意**: If側採用時に End If の行が無いことを異常・バグと誤解しないこと。分岐が正しく機能したかは
  「採用されたはずの枝のアクションが実際に `OK` で記録されているか」で判断する。
  ※旧版の本エントリでは「マーカー行は採否に関わらず記録され、End Ifだけ常に省略される」と誤って
  結論していたが、上記の2回連続実行テストにより訂正した。

---

## 2026-02-14: Tasker アクションコード code 43 = Else（Stop ではない）

- **エラー内容**: code 43 を Stop と認識してレビュー・疑似コード作成していた
- **原因**: Tasker_Action_Codes.md および外部参照元に code 43 = Stop と記載されていたが、実際は Else
- **解決方法**: Tasker_Action_Codes.md を修正（code 43 = Else）
- **備考**: 外部参照元（Taskomater/Tasker-XML-Info）の情報が不正確な場合がある。実機で確認すること

---

## 2026-06-28: Join 受信の =:= 分解と変数参照の落とし穴

- **症状**: 受信ディスパッチャで `If %joincomm ~ "verb"` が一致せず Else に落ちる／パラメータが分割されない
- **原因と正解**:
  1. `%joincomm` は**スカラー**。`%joincomm()`（括弧=配列形式）で参照すると一致しない → 括弧なし `%joincomm` で参照する
  2. `=:=` は**1個だけ**使う。複数入れると `%joincomm` が崩れる（右端トークン単体にならない）
  3. `%joinpar(1)` は `=:=` の**左をまるごと**。**Join はカンマ分割しない** → 複数引数は左側に自前の区切り（カンマ等）で詰め、受信タスクで Variable Split する
- **確定分解（=:= 1個）**: `%joincomm`＝右（コマンド/verb）/ `%joinpar(1)`＝左（全体）
- **備考**: 実機確認（Tasker 6.7.x + Join）。受け口は「プロファイル1個＋ディスパッチャ」で任意タスクを名前起動できる

---

## 2026-07-07: JavaScriptletの`setLocal()`で13桁の大きな数値を保存すると有効数字6桁に丸められる

- **エラー内容**: `Variable Set: %var: couldn't set value (invalid variable name, or non-numeric value with Do Maths on)`
- **症状**: `Date.now()`（13桁のエポックミリ秒）を`setLocal()`で保存し、別アクションで`%var`展開や`local()`で読み戻すと`1.78341e+12`のような指数表記になり、下6桁以上の精度が失われる。2秒しか離れていない2つのタイムスタンプが完全に同じ値になり区別できなくなる
- **原因**: Tasker の変数ストレージ（`setLocal()`経由）は大きな数値を保存する際に有効数字6桁程度に丸めて文字列化する。JS内で`typeof`すると`"string"`になることから、保存された時点で既にこの丸め済み文字列になっていることが確認できる（表示上の丸めではなく保存時点の丸め）。この丸められた文字列をVariable Set（Do Maths On）の数式に使うと、`e+12`形式の指数表記をパーサーが数値として解釈できずエラーになる
- **解決方法**: 13桁のエポックミリ秒をそのまま保存しない。`Date.now() % 1000000`（100万の剰余）のように6桁以下に収まる値に変換してから`setLocal()`する。100万msは約16.7分なので、それより短い時間差の計測なら桁あふれの心配なく正確な差分が取れる（実機確認: 2秒Waitで`2070ms`という妥当な差分を確認）
- **備考**: 2文字の変数名（`ta`/`tb`等）を同時に使うと「変数名は3文字以上必要」の別の罠と重なって原因の切り分けが難しくなる。まず変数名を3文字以上にした上で本事象を確認すること

---

## 2026-07-13: `%joinpar(1)` はカンマでは分割されないが、**半角スペースでは値が途中で切り詰められる**（実機確認・DocomoMailGuardのJSON破損で発覚）

- **症状**: PCから `TaskName,{"success":true,...,"errors":"some message with spaces"}=:=task` のようにスペースを含む文字列を送ると、携帯側の `%joinpar(1)` が**最初のスペースの直前まで**しか値を保持していない（それ以降は失われる）。カンマは従来通り分割されない（2026-06-28の記録は変わらず正しい）が、スペースは未検証だった
- **実機確認方法**: `Write File`アクション（code 410）でJoin_Routerに `%joinpar(1)`/分割後の`%args1`/`%args2`をログファイルへ追記するデバッグを一時的に仕込み、実際の受信値を確認して切り分けた（Flashだと直後に通知が上書きされ見づらいため、ファイル追記が有効）
- **実測例**: 送信文字列 `...,"errors":"DocomoMailGuard is already running This invocation was skipped"}=:=task` → 受信側 `%joinpar(1)` は `...,"errors":"DocomoMailGuard` で途切れる（"is"以降が消失）。JSONとして不正な形になり、受信タスク側の `JSON.parse` が異常終了、後続の通知内容がすべて空白になる
- **回避策**: Join経由でJSON等の構造化データを送る場合、値の中に半角スペースを含めない。PC側で送信前に半角スペース→アンダースコア等の別文字へ置換する
- **教訓**: Joinの`=:=`パラメータ受け渡しは「カンマは大丈夫」だけでは不十分。**スペースを含む自由文をpushで送る設計は避ける**か、事前エンコードを必須にする
- **関連**: 2026-06-28（追記2）「`=:=`の左は`%joinpar(1)`にまるごと1個（カンマ分割なし）」の記録に対する重要な補足。2026-07-10「Join_Routerのカンマ分割でJSONが千切れる」問題とは別レイヤー（あちらはTasker側のVariable Split実装バグ、今回はJoin側の受信仕様）

---

## 2026-07-14: Join `sendPush` APIの`text`パラメーターに日本語を含めると503エラー

- **エラー内容**: `https://joinjoaomgcd.appspot.com/_ah/api/messaging/v1/sendPush`へ`text`パラメーターに日本語（マルチバイト文字）を含めてリクエストすると`{"error":{"errors":[{"domain":"global","reason":"backendError","message":"backend error"}],"code":503,"message":"backend error"}}`が返る。リトライしても同じ
- **原因**: 未特定（Join側バックエンドの問題と推測）。同一パラメーター構成でtextをASCIIのみに変えたところ即座に成功したため、日本語（非ASCII文字）との相関を実機確認した
- **解決方法**: `text`パラメーターはASCII文字のみで送る。日本語を含める必要がある場合は、送信前にローマ字化するか、別の手段（本文は`file`で送る等）を検討する
- **備考**: `curl --data-urlencode`でURLエンコード自体は正しく行われている前提での事象。エンコード後の中身ではなく元の文字種（非ASCII）が原因と考えられる

---

## 2026-07-14: Joinの`file=`パラメーターはTasker側の`%joinfiles()`に値を渡さない（Tasker自動処理には使えない）

- **症状**: `sendPush?file=<Google DriveのURL>`でpushを送ると、Android端末には通知が届きJoinアプリ上でファイルを実際にダウンロード済み（Copy/Shareボタンが表示される）状態になる。しかしTasker「Join Received Push」イベント側では、Tasker公式UIの変数一覧に載っている`%joinfiles()`（配列）を含め、ファイル関連のどの変数にも値が入らない（`%joinarefileslocal`のような常時セットされる変数は`false`で解決するが、ファイル本体の情報は空）
- **原因**: 公開REST API（`sendPush`）は、Join公式クライアント（Chrome拡張機能・デスクトップアプリ）が使っている内部専用のファイル送信形式とは別物。`joaomgcd/JoinChrome`公開リポジトリのソースコード（`js/base/fileUploadProviderFactory.js`・`js/base/googleDriveManager.js`）を確認したところ、公式クライアントは「同一ローカルネットワークなら直接送信→ダメならクライアント自身のGoogle OAuthでGoogle Drive REST API(`googleapis.com/drive/v3/files`)を直接呼んでアップロード」という2段の非公開ロジックを実装しており、公開APIの`file`パラメーターとは連動していないと判明した
- **解決方法/回避策**: Tasker側で受信ファイルを自動処理（自動解凍・自動表示等）したい場合は、Joinのfile push機能に頼らず、Tasker標準の「HTTP Request」アクションでGoogle Driveの直リンクを直接GETし、保存先をTasker自身が把握する方式を使う
- **備考**: 実機確認（Tasker 6.7.6-beta + Join、2026-07-14）。検証には一時テスト用Profile/Task（Join Received Pushイベントで候補変数をFlash表示）を作成し、Tasker公式のVariable Select一覧（`%joinfiles()`, `%joinarefileslocal`等）で正しい変数名を確認した上で再検証した

---

## 2026-07-14: プロファイルXML(.prf.xml)インポート時、紐付くタスクも自動作成・更新される（インポート順序で上書きされる）

- **内容**: `.prf.xml`（プロファイル定義。内部に`<Task>`ブロックが埋め込まれている）をTaskerにインポートすると、プロファイル自体だけでなく、紐付くタスクも自動的に作成・更新される
- **注意点**: 同じタスクIDに対応する単体の`.tsk.xml`と、プロファイル埋め込みの`.prf.xml`の内容が食い違っている場合、**後からインポートした方の内容で上書きされる**。プロファイル→タスクの順でインポートすれば最終的にタスク単体ファイルの内容に、タスク→プロファイルの順なら最終的にプロファイル埋め込み内容になる
- **活用場面**: 新規タスク+プロファイルをセットで作る場合、`.prf.xml`だけをインポートすればタスクも同時に作成されるため、`.tsk.xml`を別途インポートする必要はない（内容を同期させておけば十分）。ただし2つのファイルの内容を意図的に変えて段階的にインポートする運用をする場合は、順序を明確に管理すること

---

## 2026-07-15: Scene V2 の Button コンポーネントは `text` 直接プロパティでラベルが表示されない（`content` 配列+子Text要素が必要）

- **症状**: Raw JSON で `{"type": "Button", "id": "Button1", "text": "OK", ...}` のように `text` プロパティを直接指定してインポートすると、ボタン自体は表示されるがラベル文字列が表示されない（インポート時に非対応プロパティとして無視・破棄されると見られる）
- **切り分け方**: Taskerのビジュアルエディタで新規にButtonを追加し、Raw JSONタブでその要素を確認したところ、以下の形式で生成されていた

  ```json
  {
    "type": "Button",
    "id": "Button3",
    "content": [
      { "type": "Text", "id": "Text2", "text": "Button" }
    ],
    "eventHandlers": { "...": "..." }
  }
  ```

  同一シーン内で、`text`直接指定の既存ボタン（ラベル非表示）と、`content`配列形式の新規ボタン（ラベル表示）が共存していたことで問題箇所を特定した
- **解決方法**: Buttonのラベルは `content: [{ "type": "Text", "id": "...", "text": "ラベル文字列" }]` の形式で子Text要素として持たせる。FloatingActionButtonの`content`スロットと同様の構成
- **備考**: 実機確認（Tasker 6.7.6-beta、2026-07-15）。6.7.5/6.7.6のchangelogにはこの変更に関する記載が見当たらず、いつからの変更か不明。公式マニュアル（`Tasker_ScenesV2_Official_Manual.md`）の「Button: `text`(必須)」という記載は本バージョンでは不正確なため、実機で新規Button追加時のRaw JSONを都度確認すること
- **影響**: `Tasker/Tools/sv2_helpers.js` の `button()` ヘルパーを `content` 配列形式に修正済み（2026-07-15）。過去に `text` 直接指定で生成した既存シーンのButtonは同様の症状が出る可能性があるため、該当シーンを触る際は要確認
